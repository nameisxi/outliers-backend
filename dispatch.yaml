dispatch:
  # # Send all api traffic to the default service, e.g. backend.
  # - url: "api.getoutliers.com/*"
  #   service: default

  # # Send all other traffic to the frontend.
  # - url: "*/*"
  #   service: frontend

  - url: "getoutliers.com/*"
    service: frontend
  - url: “api.getoutliers.com/*”
     service: default